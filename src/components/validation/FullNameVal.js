import React from 'react'

const FullNameVal = (fullname,grade) => {
    let errors={};

    if (!fullname) {
        errors = "Full Name Reqired."
    }


  return (
    <div>FullNameVal</div>
  )
}

export default FullNameVal